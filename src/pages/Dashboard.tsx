import { styled } from "@mui/material";

const StyledContainer = styled("div")`
  margin: 20px;
`;

const Dashboard = () => {
  return (
    <StyledContainer>
      <h1>Dashboard...</h1>
    </StyledContainer>
  );
};

export default Dashboard;
